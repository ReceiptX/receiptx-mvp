{
  "name": "receiptx-contracts",
  "version": "1.0.0",
  "description": "RWT Token Smart Contracts for ReceiptX",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:supra-testnet": "hardhat run scripts/deploy.js --network supra-testnet",
    "verify:supra": "hardhat verify --network supra-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "hardhat": "^2.19.5",
    "dotenv": "^16.4.1"
  }
}
