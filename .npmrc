# NPM Configuration for ReceiptX
# Ensures consistent builds across local and Netlify environments

# Use legacy peer dependency resolution (fixes supra-l1-sdk conflicts)
legacy-peer-deps=true

# Increase network timeout for slow connections
fetch-timeout=600000
fetch-retries=5

# Production optimizations
engine-strict=false
audit=false
fund=false
